version: "3.9"

services:
  zuultimate:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - zuul-data:/app/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  zuul-data:
