# Zuultimate Configuration
# Copy to .env and adjust values. All settings use the ZUUL_ prefix.

# ── Core ──
ZUUL_ENVIRONMENT=development
ZUUL_SECRET_KEY=change-me-to-a-random-secret-key

# ── Database URLs (defaults to SQLite) ──
# ZUUL_IDENTITY_DB_URL=sqlite+aiosqlite:///./data/identity.db
# ZUUL_CREDENTIAL_DB_URL=sqlite+aiosqlite:///./data/credentials.db
# ZUUL_SESSION_DB_URL=sqlite+aiosqlite:///./data/sessions.db
# ZUUL_TRANSACTION_DB_URL=sqlite+aiosqlite:///./data/transactions.db
# ZUUL_AUDIT_DB_URL=sqlite+aiosqlite:///./data/audit.db
# ZUUL_CRM_DB_URL=sqlite+aiosqlite:///./data/crm.db

# ── Redis (optional — falls back to in-memory) ──
# ZUUL_REDIS_URL=redis://localhost:6379/0

# ── AI Security ──
ZUUL_REDTEAM_PASSPHRASE=

# ── Crypto salts (override per deployment) ──
# ZUUL_VAULT_SALT=zuultimate-vault-v2
# ZUUL_MFA_SALT=zuultimate-mfa-secret
# ZUUL_PASSWORD_VAULT_SALT=zuultimate-pw-vault

# ── SSO ──
# ZUUL_SSO_ALLOWED_REDIRECT_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ── API ──
# ZUUL_API_TITLE=Zuultimate
# ZUUL_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]
# ZUUL_MAX_REQUEST_BYTES=1048576

# ── Auth / tokens ──
# ZUUL_ACCESS_TOKEN_EXPIRE_MINUTES=60
# ZUUL_REFRESH_TOKEN_EXPIRE_DAYS=7
# ZUUL_LOGIN_RATE_LIMIT=10
# ZUUL_LOGIN_RATE_WINDOW=300

# ── AI Security tuning ──
# ZUUL_MAX_AUDIT_EVENTS=10000
# ZUUL_THREAT_SCORE_THRESHOLD=0.3

# ── Service-to-service auth (for Vinzy → Zuultimate calls) ──
ZUUL_SERVICE_TOKEN=change-me-to-a-random-service-token
